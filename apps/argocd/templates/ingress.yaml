apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: argocd-ingress
  namespace: argocd
spec:
  entryPoints:
    - webscure
  routes:             
  - kind: Rule
    match: Host(`argocd.arcamh.com`)
    services:
    - kind: Service
      name: argocd-server
      namespace: argocd
