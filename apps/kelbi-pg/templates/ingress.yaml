apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: kelbi-pg-ingress
  namespace: kelbi-pg
spec:
  entryPoints:
    - websecure
    - web
  routes:
    - kind: Rule
      match: Host(`zsoqnqojnmxl.arcamh.com`)
      priority: 10
      services:
        - name: kelbi-pg-root-app-kelbi-pg-dev
          namespace: kelbi-pg
          port: 5432