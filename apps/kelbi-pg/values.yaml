postgresql:
  nameOverride: kelbi-pg-dev
  primary:
    service:
      type: LoadBalancer
  backup:
    enabled: true 
    timeZone: "America/Sao_Paulo"
    cronjob:
      storage:
        size: 4Gi
        storageClass: "local-path"